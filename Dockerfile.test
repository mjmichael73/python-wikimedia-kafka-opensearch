FROM python:3.11-slim

WORKDIR /app

COPY requirements-dev.txt .
RUN pip install --no-cache-dir -r requirements-dev.txt

# Project source is mounted at runtime by docker-compose (test-runner service).
CMD ["pytest", "tests/", "-v"]
